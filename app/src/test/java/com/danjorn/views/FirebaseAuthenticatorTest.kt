package com.danjorn.views

import com.google.firebase.database.DataSnapshot
import org.hamcrest.CoreMatchers.`is`
import org.hamcrest.MatcherAssert.assertThat
import org.junit.Test

class FirebaseAuthenticatorTest {

 /*   //interface Authenticator { fun userExists(user): Bool; fun signUpUser(user): Eith<F, U>
    //class FbAuth : Authenticator { fun userExists(user) { ... }; fun signUpUser(user) { ... }

    private lateinit var mockedMainFb: RealFb
    private lateinit var userDataSnapshot: DataSnapshot

    @Test
    fun whenUserNotExist_userExistsReturnsFalse(){
        val notExistingName = "not_exist"
        val user = Userr(notExistingName)

        whenCall(mockedMainFb.query(FBConfig.usernameKey, notExistingName)).thenReturn(null)

        val firebaseAuth = fbAuth(mockedFb)
        val result = fbAuth.userExist(user)
        assertThat(result, `is`(false))
    }*/
}